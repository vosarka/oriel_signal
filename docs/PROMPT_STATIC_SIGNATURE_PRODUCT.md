Read AGENTS.md, docs/VISUAL_LAW.md, docs/ORIEL_VISUAL_LANGUAGE.md, and docs/PLATFORM_STRUCTURE_AND_FLOW.md first. All absolute. Work on branch v2-baseline. Plan first, build in passes, verify with pnpm dev, self-critique, show diff, wait for approval before each commit. Log in wiki/log.md.

GOAL: Complete the ORIEL Static Signature product — the product page + the document generation system. Most of this ALREADY EXISTS. Do NOT rebuild from scratch. Connect, finish, and re-skin what is there.

═══════════════════════════════════════════════════════
WHAT ALREADY EXISTS (read these first, report what you find)
═══════════════════════════════════════════════════════
- server/signature-letter-system.ts — defines TWO products: "glimpse" (€23.58, 2-3 pages, 2 codons, 1 correction) and "founding" (€81.32, 8-12 pages, 6 codons, 3 corrections, followup). 12 order statuses from pending_payment → delivered.
- server/signature-letter-service.ts — the generation service.
- client/src/pages/SignatureProductPage.tsx (215 ln), SignatureIntake.tsx (329 ln), FoundingSignatureLetter.tsx (163 ln).
- codex/vrc_static_signature/10_REPORT_BUILDER/ — the FULL document spec:
  - OSSB_15_Page_Content_Structure.md (the 15-page content)
  - REPORT_FLOW.md (layout, dark/light contrast pacing, page compositions)
  - PDF_SCHEMA.md (the data schema)
  - CODON_CARDS.md (codon card layouts)
  - mockup.html (a visual mockup)
- The VRC engines already compute the signature data (codons, centers, facets, prime stack, SLI) — verified 177/178 tests pass.

Start by reading signature-letter-system.ts, signature-letter-service.ts, REPORT_FLOW.md, OSSB_15_Page_Content_Structure.md, and PDF_SCHEMA.md fully. Then tell me, in your plan: what is already wired, what is stubbed/incomplete, and what is missing for an end-to-end flow. Do NOT assume — verify in the code.

═══════════════════════════════════════════════════════
PART A — THE PRODUCT PAGE (re-skin + complete)
═══════════════════════════════════════════════════════
Bring the Static Signature product page into the homepage's world (obsidian + sacred geometry + holographic light + the SignalPageShell). It should:
1. Present the product as a recovered archival artifact — "Your Static Signature Blueprint: a precise map of the structure you arrived with."
2. Show the two tiers clearly as SIGNAL CLEARANCE levels: the Glimpse (€23.58) and the Founding Signature Letter (€81.32) — using the real prices/specs from SIGNATURE_PRODUCTS, not invented ones.
3. Explain what the buyer receives (page count, codons decoded, micro-corrections, founder letter, followup) pulled from the product config.
4. Lead to the existing checkout/intake flow (do not rebuild payment — PayPal infra exists; wire to it).
Use VISUAL_LAW + VISUAL_LANGUAGE strictly: no brown, no certificate frames, gold structure, holographic light as event. Reuse GlowCard / SignalButton / shared components.

═══════════════════════════════════════════════════════
PART B — THE DOCUMENT GENERATION (the core deliverable)
═══════════════════════════════════════════════════════
Complete the pipeline that turns a paid order + intake data into the actual document, following the 15-page spec in 10_REPORT_BUILDER/.

Flow (per signature-letter-system.ts statuses):
paid → intake_received → signature_generated (VRC engine computes the data) → draft_ready (ORIEL writes the letter copy) → pdf_ready (rendered document) → delivered.

Tasks:
1. Verify the VRC engine output feeds the document data schema (PDF_SCHEMA.md). Map engine result → schema fields. Report any gaps.
2. Render the document following REPORT_FLOW.md: the 15-page structure, dark/light contrast pacing, the page compositions (twin-wheel 88° solar arc, vertical prime-stack spine, silhouette body map, codon cards, radar/distortion map, ORIEL voice page). Use the SAME visual language as the site (obsidian, gold, cyan, sacred geometry) so the PDF feels part of the world.
3. For PDF generation: check what's available in the repo first. If a renderer (Puppeteer/Playwright/react-pdf) is already present, use it; if not, recommend the lightest option and ASK before adding a dependency. The mockup.html suggests an HTML→PDF route (render the styled HTML, print to PDF) — likely the cleanest path.
4. The ORIEL letter copy (the personalized founder's voice section) is generated by the existing signature-letter-service — confirm it runs and produces the tone variants (mystical/practical/balanced).
5. End-to-end test with a sample order: paid → intake → generate → render → a real PDF file output. Show me the result.

═══════════════════════════════════════════════════════
CONSTRAINTS & PROCESS
═══════════════════════════════════════════════════════
- Touch presentation + the document pipeline only. Do NOT change the VRC engines, auth, or unrelated routers. Do NOT alter the codon math.
- No brown, no AI-generated images, no certificate framing, no new dependencies without asking, no reformatting unrelated files.
- Respect the real product config (prices, page counts, limits) — never invent product details.
- Do Part A first (visible, lower risk), verify, commit. Then Part B in stages: data mapping → render → PDF → end-to-end, showing the plan and diff at each stage, waiting for approval.
- prefers-reduced-motion respected on the product page. Self-critique each pass: same world? darkness dominant? real product data? document matches the 15-page spec? anything brown?

Begin by reading the files listed above and giving me: (1) the current state (wired/stubbed/missing), and (2) your staged plan for Part A and Part B. Wait for my approval before building.
